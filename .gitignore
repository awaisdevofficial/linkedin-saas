# Dependencies
node_modules

# Environment (never commit secrets)
.env
.env.local
.env.*.local

# Build output
dist
frontend/dist

# Legacy (removed)
postora-extension

# Editor / OS
.DS_Store
*.log
*.tsbuildinfo

# Optional: keep backend and frontend .gitignore rules in sync with this file
